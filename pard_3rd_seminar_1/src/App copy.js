import { useState, useRef } from "react";

function Week2_useRef(){
  const [state, setState] = useState(0);
  const ref = useRef(0);

  const
  return(
    <div>
      <Name/>
      <Email/>
      <Message/>
      <Button/>
    </div>
  );
}

export default App;